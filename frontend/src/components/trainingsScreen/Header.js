import React from "react";
import { View, Text, StatusBar } from "react-native";

const Header = () => {
  return (
    <View className="mt-3">
      <Text className="text-textPrimary text-32 font-bold">Trainings</Text>
    </View>
  );
};

export default Header;
